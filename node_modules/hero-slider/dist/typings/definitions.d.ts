import { BackdropFilterProperty, BackfaceVisibilityProperty, BackgroundProperty, BackgroundAttachmentProperty, BackgroundBlendModeProperty, BackgroundClipProperty, BackgroundColorProperty, BackgroundImageProperty, BackgroundOriginProperty, BackgroundPositionProperty, BackgroundPositionXProperty, BackgroundPositionYProperty, BackgroundRepeatProperty, BackgroundSizeProperty, WidthProperty, HeightProperty } from 'csstype';
import IntervalTimer from '../dependencies/IntervalTimer';
declare type TAnyFunction = (...anyArg: any[]) => any;
/**
 * `INavbarSettings` settings definition for all of the nav components.
 */
interface INavbarSettings {
    color: string;
    activeColor: string;
}
/**
 * `EAnimations` enum for the different sliding animations.
 */
declare enum EAnimations {
    TOP_TO_BOTTOM = "top_to_bottom",
    BOTTOM_TO_TOP = "bottom_to_top",
    LEFT_TO_RIGHT = "left_to_right",
    RIGHT_TO_LEFT = "right_to_left",
    FADE = "fade"
}
import { EAutoplayButtons, IAutoplayButtonProps } from '../components/AutoplayButtons/typings';
import { IButtonProps } from '../components/Buttons/typings';
import { IButtonsNavProps } from '../components/ButtonsNav/typings';
import { ISliderContext, EActionTypes, IReducerState, IReducerAction, IReducerSlideAction, ISlidePayload, IReducerSetSlideNumbereAction, ISlideDataPayload, IReducerNavAction, INavPayload, IReducerAutoplayButtonAction, IReducerAutoplayButtonPayload, ISliderProviderProps, IWithProviderProps } from '../components/Context/typings';
import { IMenuNavProps } from '../components/MenuNav/typings';
import { INavPosition, INavSettings, INavProps } from '../components/Nav/typings';
import { ISideNavProps } from '../components/SideNav/typings';
import { ISlideProps } from '../components/Slide/typings';
import { EBackgroundAnimations, IBackgroundProps } from '../components/Slide/Background/typings';
import { IMaskProps } from '../components/Slide/Mask/typings';
import { ISliderProps, AnimationType } from '../components/Slider/typings';
export { IntervalTimer, TAnyFunction, BackdropFilterProperty, BackfaceVisibilityProperty, BackgroundProperty, BackgroundAttachmentProperty, BackgroundBlendModeProperty, BackgroundClipProperty, BackgroundColorProperty, BackgroundImageProperty, BackgroundOriginProperty, BackgroundPositionProperty, BackgroundPositionXProperty, BackgroundPositionYProperty, BackgroundRepeatProperty, BackgroundSizeProperty, WidthProperty, HeightProperty, INavbarSettings, EAnimations, EAutoplayButtons, IAutoplayButtonProps, IButtonProps, IButtonsNavProps, ISliderContext, EActionTypes, IReducerState, IReducerAction, IReducerSlideAction, ISlidePayload, IReducerSetSlideNumbereAction, ISlideDataPayload, IReducerNavAction, INavPayload, IReducerAutoplayButtonAction, IReducerAutoplayButtonPayload, ISliderProviderProps, IWithProviderProps, IMenuNavProps, INavPosition, INavSettings, INavProps, ISideNavProps, ISlideProps, EBackgroundAnimations, IBackgroundProps, IMaskProps, ISliderProps, AnimationType, };
