import { EAnimations } from '../../typings/definitions';
interface StyledSlideProps {
    isActive: boolean;
    isDoneSliding: boolean;
    slidingAnimation: EAnimations;
}
export declare const StyledSlide: import("styled-components").StyledComponent<"div", any, StyledSlideProps, never>;
export {};
